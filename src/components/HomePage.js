import React from "react";

const HomePage = () => (
  <div className="container">
    <h1 className="primary-heading">BookInfo</h1>
    <h3 className="subheading">
      Search for a book or an author and find information.
    </h3>
  </div>
);

export default HomePage;
